import 'package:flutter/material.dart';

class BaseScreenTablet extends StatelessWidget {
  const BaseScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Tablet Screen")));
  }
}
